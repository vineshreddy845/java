public class Stringoperation {
    public static void main(String args[]) {
        // string methods => length,toUpperCase(),toLowerCase(),text.indexOf(),string
        // concatention.
        String name = "vinesh";
        String name1 = "reddy";
        System.out.println("name" + name.length()); // length operation
        System.out.println("name1" + name1.toUpperCase());//
        System.out.println("name" + name.toLowerCase());
        // System.out.println("name" + name.indexOf());//
        /*
         * \n=> Newline
         * \r=>carriage return
         * \t => Tab
         * \b => backspace
         * \f => from feed
         * Math.max(x,y)
         * Math.min(x,y)
         * Math.sqrt(x)
         * Math.abs(x)
         * Math.random()
         */
    }

}
